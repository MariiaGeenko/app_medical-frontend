import { Typography } from 'antd';
const { Title } = Typography;

export const Home = () => {
    return (
        <div>
              <Title level={5} style={{textAlign: 'center'}}>Добро пожаловать в программу my-receipt</Title> 
        </div>   
        )
}